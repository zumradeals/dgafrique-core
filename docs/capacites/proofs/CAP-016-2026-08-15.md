# Preuve préproduction — CAP-016

Date : 2026-08-15  
Capacité : **CAP-016 — Accompagnement DG Afrique**

## Déploiement observé

La branche `cap-016-accompagnement-dg-afrique` a été validée sur le VPS de préproduction après les commits :

- `d7b3cce` — `Build CAP-016 project accompaniment`
- `2018757` — `Harden CAP-016 route loading`
- `da61882` — `Fix CAP-016 web session middleware`

La migration `2026_08_15_234000_create_project_accompaniment_tables` avait déjà été appliquée avec succès lors de la première validation du lot.

## Routage et session

`php8.4 artisan route:list --path=accompagnement -vv`

Résultat : 6 routes CAP-016 chargées. Les routes membre et administration passent par la pile `web`, notamment `StartSession`, `PreventRequestForgery`, `SubstituteBindings`, puis les gardes `RequireCoreMember` et, pour l'administration, `RequirePortalAdministrator`.

## Tests ciblés

`php8.4 artisan test --filter=ProjectAccompanimentTest`

Résultat :

- **6 tests réussis**
- **22 assertions**

Les preuves couvrent :

- activation et fin par le porteur sans transfert de contrôle ;
- interventions internes et partenaires tracées comme faits append-only ;
- refus d'activation par un tiers ;
- enregistrement d'intervention réservé à un administrateur provisionné ;
- orientation vers un mécanisme de financement sans création de finance ni changement de propriété ;
- configuration administrative des catégories sans redéploiement.

## Non-régression

`php8.4 artisan test`

Résultat :

- **109 tests réussis**
- **587 assertions**
- durée observée : **3,53 s**

## Optimisation et HTTPS

`php8.4 artisan optimize` a terminé avec succès pour la configuration, les événements, les routes et les vues.

`curl -I https://beta.dgafrique.com`

Résultat :

`HTTP/1.1 200 OK`

## Conclusion

CAP-016 est **validé en préproduction** sur `da61882`. L'accompagnement DG Afrique est progressif et traçable, peut être interne ou coordonné avec un partenaire, et ne transfère ni propriété, ni contrôle, ni maturité, ni financement au projet.
