# Preuve CAP-007A/B — Programme ZUMRA et adhésion sécurisée

## Décision

**VALIDÉ EN PRÉPRODUCTION le 2026-08-14.**

Cette validation couvre le dossier d'adhésion, la charte versionnée, le
registre des tentatives de paiement, la confirmation serveur, l'activation
atomique et idempotente ainsi que le reçu privé.

Elle ne vaut pas ouverture marchande : `ZUMRA_PAYMENT_ENABLED` demeure à
`false` jusqu'à la preuve de bout en bout avec le compte GeniusPay live.

## Environnement éprouvé

- branche : `main` ;
- commit fonctionnel éprouvé : `3034488` ;
- URL de préproduction : `https://beta.dgafrique.com` ;
- runtime : PHP 8.4, Laravel 13, PostgreSQL, Redis et Nginx ;
- migration CAP-007B exécutée :
  `2026_08_14_210000_create_zumra_membership_payment_tables`.

## Preuve automatisée ciblée

```bash
php8.4 artisan test --filter=ZumraMembershipPaymentTest
```

Résultat : **4 tests réussis, 36 assertions, 0 échec**.

La preuve ciblée démontre :

1. l'enregistrement d'une tentative live avant la redirection ;
2. l'activation uniquement après lecture serveur d'un état final ;
3. l'idempotence de l'activation, de l'événement et du reçu ;
4. l'impossibilité d'activer avec un paramètre navigateur falsifié ;
5. l'isolation du reçu par identité canonique.

## Preuve de non-régression complète

```bash
php8.4 artisan test
```

Résultat : **55 tests réussis, 278 assertions, 0 échec**, puis optimisation
Laravel réussie.

## Gate d'exploitation restant

Avant tout encaissement réel : installer les secrets live hors dépôt, vérifier
le contrat officiel du prestataire, effectuer un paiement contrôlé de 500 FCFA,
constater la confirmation serveur, le passage unique à `ACTIVE` et le reçu,
puis seulement conserver `ZUMRA_PAYMENT_ENABLED=true`.
