<!doctype html>
<html lang="fr">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="robots" content="noindex, nofollow">
    <title>DG Afrique — Fondation</title>
    <style>
        :root { color-scheme: dark; font-family: Inter, ui-sans-serif, system-ui, sans-serif; }
        * { box-sizing: border-box; }
        body { margin: 0; min-height: 100vh; color: #f7faf9; background: #061b2d; }
        main { min-height: 100vh; display: grid; place-items: center; padding: 2rem; overflow: hidden; position: relative; }
        main::before { content: ''; position: absolute; width: 34rem; height: 34rem; border-radius: 50%; background: #18c77a; filter: blur(140px); opacity: .18; top: -14rem; right: -8rem; }
        .panel { width: min(76rem, 100%); position: relative; padding: clamp(2rem, 6vw, 5rem); border: 1px solid rgba(255,255,255,.12); border-radius: 2rem; background: linear-gradient(135deg, rgba(255,255,255,.085), rgba(255,255,255,.025)); box-shadow: 0 2rem 7rem rgba(0,0,0,.35); }
        .eyebrow { color: #5ee6a9; font-weight: 800; letter-spacing: .16em; text-transform: uppercase; font-size: .76rem; }
        h1 { max-width: 14ch; font-size: clamp(3rem, 8vw, 7rem); letter-spacing: -.065em; line-height: .92; margin: 1rem 0 1.5rem; }
        p { max-width: 48rem; color: #bed0da; font-size: clamp(1rem, 2vw, 1.25rem); line-height: 1.7; }
        .status { display: inline-flex; align-items: center; gap: .65rem; margin-top: 2rem; padding: .75rem 1rem; border-radius: 999px; color: #d9fff0; background: rgba(24,199,122,.13); border: 1px solid rgba(94,230,169,.3); font-weight: 700; }
        .dot { width: .7rem; height: .7rem; border-radius: 50%; background: #2ee58c; box-shadow: 0 0 1.2rem #2ee58c; }
    </style>
</head>
<body>
<main>
    <section class="panel">
        <div class="eyebrow">African First · Préproduction</div>
        <h1>DG Afrique se reconstruit.</h1>
        <p>Le nouveau portail prend forme sur une fondation Laravel, PostgreSQL et Redis, avec GAMAD Core comme autorité d’identité unique.</p>
        <div class="status"><span class="dot"></span> Fondation CAP‑001 en cours</div>
    </section>
</main>
</body>
</html>
