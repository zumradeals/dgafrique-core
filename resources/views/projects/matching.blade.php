<x-layouts.portal title="Correspondances — {{ $project->name }}"><main class="project-page"><div class="match-content"><nav><a href="{{ route('projects.show',$project) }}">← Retour au projet</a><a href="{{ route('member.space') }}">Mon espace</a></nav><header><p class="eyebrow dark">CAPACITÉS ↔ PROJET</p><h1>{{ $configuration['page_title'] }}</h1><p>{{ $configuration['page_intro'] }}</p><strong>{{ $project->name }}</strong></header>@if(session('status'))<div class="alert success">{{ session('status') }}</div>@endif<div class="match-honesty"><b>Orientation, pas affectation</b><span>Chaque résultat repose sur une capacité volontairement découvrable. Aucun contact, rôle ou engagement n’est créé.</span></div>@if($matches===[])<section class="project-empty"><i>↔</i><h2>Aucune correspondance consentie</h2><p>Précisez les capacités recherchées ou attendez que des membres choisissent de rendre leurs capacités découvrables.</p></section>@else<section class="match-grid">@foreach($matches as $match)<article><div class="match-person"><i>{{ mb_strtoupper(mb_substr($match['profile']->discovery_display_name,0,1)) }}</i><div><h2>{{ $match['profile']->discovery_display_name }}</h2><p>{{ $match['profile']->activity ?: 'Activité non précisée' }}{{ $match['profile']->city ? ' · '.$match['profile']->city : '' }}</p></div></div><div class="match-reasons"><strong>Pourquoi cette piste ?</strong><ul>@foreach($match['reasons'] as $reason)<li>{{ $reason }}</li>@endforeach</ul></div><footer><a href="{{ route('people.show',$match['profile']->discovery_reference) }}">Voir le profil public</a><form method="POST" action="{{ route('projects.matching.hide',[$project,$match['profile']->discovery_reference]) }}">@csrf<button>Masquer cette piste</button></form></footer></article>@endforeach</section>@endif</div></main></x-layouts.portal>
