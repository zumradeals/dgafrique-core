<x-layouts.portal title="Projets — DG Afrique"><main class="project-page"><div class="project-content"><header class="project-hero"><div><p class="eyebrow dark">DU BESOIN À L’ACTION</p><h1>{{ $configuration['directory_title'] }}</h1><p>{{ $configuration['directory_intro'] }}</p></div><a href="{{ route('projects.create') }}">Proposer un projet</a></header><nav class="project-nav"><a href="{{ route('member.space') }}">← Mon espace</a><a href="{{ route('needs.index') }}">Besoins</a><a href="{{ route('zumra.groups.index') }}">ZUMRA</a></nav><form class="project-filter"><select name="domain"><option value="">Tous les domaines</option>@foreach($configuration['domains'] as $code=>$label)<option value="{{ $code }}" @selected(request('domain')===$code)>{{ $label }}</option>@endforeach</select><button>Explorer</button></form>@if($projects->isEmpty())<section class="project-empty"><i>P</i><h2>Aucun projet visible</h2><p>Une proposition structurée apparaîtra ici sans inventer de résultats ni de financement.</p></section>@else<section class="project-grid">@foreach($projects as $project)<article class="project-card"><div><span>{{ $configuration['domains'][$project->domain]??$project->domain }}</span><b>{{ ['PROPOSED'=>'Proposé','ADOPTED'=>'Adopté','IN_PROGRESS'=>'En action','COMPLETED'=>'Réalisé'][$project->status]??$project->status }}</b></div><h2>{{ $project->name }}</h2><p>{{ $project->summary }}</p><dl><dt>Maturité</dt><dd>{{ str_replace('_',' ',mb_strtolower($project->maturity)) }}</dd><dt>Porteur</dt><dd>{{ $project->owner_type==='GROUP'?($groups->get($project->owner_reference)?->name??'ZUMRA'):'Projet personnel' }}</dd></dl><a href="{{ route('projects.show',$project) }}">Ouvrir le dossier →</a>@if(in_array($project->id,$manageableProjectIds,true))<a href="{{ route('projects.matching',$project) }}">Trouver des capacités →</a>@endif</article>@endforeach</section>{{ $projects->links() }}@endif</div></main></x-layouts.portal>
