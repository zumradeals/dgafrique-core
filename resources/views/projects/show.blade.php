<x-layouts.portal title="{{ $project->name }} — DG Afrique"><main class="project-page"><div class="project-detail"><nav><a href="{{ route('projects.index') }}">← Projets</a><a href="{{ route('member.space') }}">Mon espace</a></nav>@if(session('status'))<div class="alert success">{{ session('status') }}</div>@endif @if($errors->any())<div class="alert danger">{{ $errors->first() }}</div>@endif<header><div><p class="eyebrow">{{ $configuration['domains'][$project->domain]??$project->domain }}</p><h1>{{ $project->name }}</h1><p>{{ $project->owner_type==='GROUP'?($group?->name??'ZUMRA'):'Projet personnel accompagné' }}</p></div><aside><span>{{ $project->status }}</span><strong>{{ str_replace('_',' ',mb_strtolower($project->maturity)) }}</strong><small>Maturité indicative, non juridique</small></aside></header><section class="project-story"><article><small>LE PROBLÈME</small><p>{{ $project->problem }}</p></article><article><small>LA RÉPONSE ENVISAGÉE</small><p>{{ $project->proposed_solution }}</p></article><article><small>LES BÉNÉFICIAIRES</small><p>{{ $project->beneficiaries }}</p></article></section><section class="project-map"><div><h2>Ce que le projet veut accomplir</h2><ul>@foreach($project->objectives as $item)<li>{{ $item }}</li>@endforeach</ul></div><div><h2>Capacités nécessaires</h2><ul>@forelse($project->required_capabilities as $item)<li>{{ $item }}</li>@empty<li>À préciser</li>@endforelse</ul></div><div><h2>Ressources nécessaires</h2><ul>@forelse($project->required_resources as $item)<li>{{ $item }}</li>@empty<li>À préciser</li>@endforelse</ul></div></section><section class="project-steps"><h2>Chemin envisagé</h2>@foreach($project->milestones as $step)<article><i>{{ str_pad((string)$step->position,2,'0',STR_PAD_LEFT) }}</i><span>{{ $step->title }}</span><small>{{ $step->status==='COMPLETED'?'Réalisée':'Prévue' }}</small></article>@endforeach</section><div class="project-finance-boundary"><strong>Aucun financement ouvert</strong><span>Cette fiche organise le projet ; elle ne constitue ni une collecte, ni une promesse d’accompagnement.</span></div>@if($canDecide)<section class="project-decisions"><h2>Faire évoluer le dossier</h2><div>@if($project->owner_type==='GROUP'&&$project->status==='PROPOSED')<form method="POST" action="{{ route('projects.transition',$project) }}">@csrf @method('PUT')<input type="hidden" name="status" value="ADOPTED"><button>Adopter pour la ZUMRA</button></form>@endif @if(in_array($project->status,['PROPOSED','ADOPTED']))<form method="POST" action="{{ route('projects.transition',$project) }}">@csrf @method('PUT')<input type="hidden" name="status" value="IN_PROGRESS"><select name="maturity"><option value="EXPLORATION">Exploration</option><option value="TEAM">Équipe</option><option value="EXPERIMENT">Expérimentation</option><option value="STRUCTURED">Structuré</option></select><button>Démarrer</button></form>@endif @if($project->status==='IN_PROGRESS')<form method="POST" action="{{ route('projects.transition',$project) }}">@csrf @method('PUT')<input type="hidden" name="status" value="COMPLETED"><input type="hidden" name="maturity" value="ACTIVITY"><button>Marquer réalisé</button></form>@endif<form method="POST" action="{{ route('projects.transition',$project) }}">@csrf @method('PUT')<input type="hidden" name="status" value="ARCHIVED"><button class="quiet">Archiver</button></form></div></section>@endif</div></main></x-layouts.portal>
